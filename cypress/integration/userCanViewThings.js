describe("User van view empllyes", () => {
    beforeEach(() => {
      cy.visit("/");
      
    });
  
    it("is expected to display 10 employees", () => {
      cy.get("[data-cy'employee-list]").should(
        
      );
    });
  });
  
  
  

